DOM: Document object Module

Es un objeto que el navegador web va a crear y que va a tener toda la estrucutra de nuestra pagina por ejemplo todos los elementos que vamos a tener. 
Este DOM, nos va a permitir acceder a todos los elementos para editarlos o eliminarlos, nos va a permitir cambiar los estilos CSS  